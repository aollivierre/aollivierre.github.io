# Set your Git email (use the same email as your GitHub account)
git config --global user.email "AbdullahOllivierre@gmail.com"

# Set your Git name
git config --global user.name "aollivierre"

# Verify the settings
git config --global --list